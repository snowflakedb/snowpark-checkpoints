{% set name = snowpark.checkpoints.hypothesis" %}
{% set version = "0.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  # need to use path to build on Jenkins
  path: ../

build:
  skip: True
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vvv

requirements:
  host:
    - python<3.12,>=3.9
    - pip
    - wheel
    - hatchling
  run:
    - python<3.12,>=3.9
    - hypothesis
    - pandera[io]==0.20.4
    - snowflake-snowpark-python[localtest]

test:
  imports:
    - snowpark.checkpoints.hypothesis

about:
  summary: snowpark.checkpoints.hypothesis
